import React from 'react'
import Button from './Button'

function HomeMain(props) {
    return(
        <main className="App-buttons">
            <Button text="Sign Up" />
            <Button text="Login" />
        </main> 
    )
} 

export default HomeMain;
